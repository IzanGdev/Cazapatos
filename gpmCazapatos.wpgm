import wollok.game.*
import juego.*
import mira.*
import aves.*




program programCazapatos {
    game.width(18)
    game.height(18)
    game.boardGround("fondo.png")
    game.addVisual(menu)
    game.title("Duckhunt")

   

    game.start()
    menu.iniciarJuego()
}
